import { H1 } from "./default/headings";

export function Header() {
    return (
        <div>
            <H1>Discord OAuth Privacy Rewrites</H1>
            <span>Manage which OAuth scopes should be automatically removed.</span>
        </div>
    );
}
